(function (global, factory) {
  typeof exports === 'object' && typeof module !== 'undefined' ? factory(exports, require('graphology')) :
  typeof define === 'function' && define.amd ? define(['exports', 'graphology'], factory) :
  (global = typeof globalThis !== 'undefined' ? globalThis : global || self, factory(global.graphologyGexf = {}, global.graphology));
}(this, (function (exports, graphology) {
  'use strict';

  function parse(graphology, gexfString) {
    const parser = new DOMParser();
    const xml = parser.parseFromString(gexfString, 'application/xml');
    const graphElement = xml.querySelector('graph');

    const graph = new graphology.Graph();

    const nodes = xml.querySelectorAll('node');
    nodes.forEach(node => {
      const id = node.getAttribute('id');
      const label = node.getAttribute('label');
      graph.addNode(id, { label });
    });

    const edges = xml.querySelectorAll('edge');
    edges.forEach(edge => {
      const source = edge.getAttribute('source');
      const target = edge.getAttribute('target');
      const id = edge.getAttribute('id') || source + '-' + target;
      graph.addEdge(source, target, { id });
    });

    return graph;
  }

  exports.parse = parse;

  Object.defineProperty(exports, '__esModule', { value: true });
})));
